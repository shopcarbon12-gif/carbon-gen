import { SectionShell } from "@/components/section-shell";

export default function StudioSocialPage() {
  return (
    <SectionShell
      title="Ad Generator"
      description="Create campaign-ready assets for Meta and Google Display without mixing this workflow into the Image Studio workspace."
    />
  );
}
