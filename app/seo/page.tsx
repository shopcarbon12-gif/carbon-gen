import { SectionShell } from "@/components/section-shell";

export default function SeoPage() {
  return (
    <SectionShell
      title="SEO"
      description="Metadata workflow for title, description, and image alt-text generation."
    />
  );
}
