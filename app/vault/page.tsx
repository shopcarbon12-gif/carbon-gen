import { SectionShell } from "@/components/section-shell";

export default function VaultPage() {
  return (
    <SectionShell
      title="Vault"
      description="Saved assets and generation history for reuse and publishing."
    />
  );
}
