import { SectionShell } from "@/components/section-shell";

export default function OpsInventoryPage() {
  return (
    <SectionShell
      title="Collection Mapper"
      description="Map Lightspeed inventory to Shopify manual collections in a dedicated operations workspace."
    />
  );
}
