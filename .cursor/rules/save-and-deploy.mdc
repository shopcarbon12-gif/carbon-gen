---
description: MUST deploy to Vercel after every code change. No exceptions.
alwaysApply: true
---

# Deploy After Every Change (LOCKED)

**MANDATORY:** After making ANY code changes to this project, you MUST deploy.

## Deploy (required)
```
npm run deploy:vercel
```

Run this immediately after completing changes. Do not ask if the user wants to deploy — just deploy.

Skip only if the user explicitly says "don't deploy", "no deploy", or "skip deploy".
